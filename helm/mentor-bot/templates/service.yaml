apiVersion: v1
kind: Service
metadata:
  name: {{ include "mentor-bot.fullname" . }}
  labels:
    {{- include "mentor-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mentor-bot.selectorLabels" . | nindent 4 }}